# Implementation Plan: Forge E2E Test Strategy

**Track ID:** e2e-test-strategy_20260512
**Spec:** [spec.md](./spec.md)
**Created:** 2026-05-12
**Status:** [ ] Not Started

## Overview

Organize the existing test coverage into explicit layers, add stable npm scripts for those layers, and document the difference between deterministic mocked e2e and optional live task-board smoke checks.

## Phase 1: Test Taxonomy <!-- checkpoint:0661a27 -->

Classify the current test files and make the command surface match how failures should be interpreted.

### Tasks

- [x] Task 1.1: Update `package.json` scripts to add named commands for contract/runtime checks, MCP checks, mocked board integration, deterministic e2e, and full local regression. <!-- sha:b568265 -->
  - [ ] Keep `test` as the full local regression command using `node --test`.
  - [ ] Keep or replace `test:runtime` only if the new naming remains backward-compatible enough for current users.
- [x] Task 1.2: Map existing tests into the new scripts without changing test behavior: `tests/contract-validator.test.mjs`, `tests/transition-policy.test.mjs`, `tests/forge-runner.test.mjs`, `tests/mcp-server.test.mjs`, `tests/task-source-*.test.mjs`, `tests/task-label-sync.test.mjs`, `tests/forge-board-labels.test.mjs`, and git/run-store/prompt/context tests. <!-- sha:b568265 -->
- [x] Task 1.3: Add a short script comment-equivalent in README rather than JSON comments, because `package.json` cannot carry comments. <!-- sha:b6ef865 -->

### Verification

- [x] `npm.cmd run test:core` passes.
- [x] `npm.cmd run test:mcp` passes.
- [x] `npm.cmd run test:board:mock` passes.
- [x] `npm.cmd run test:e2e` passes.
- [x] `npm.cmd test` passes.

## Phase 2: Live Board Boundary <!-- checkpoint:31d351e -->

Keep real tracker checks outside the default regression path and guard them against accidental side effects.

### Tasks

- [x] Task 2.1: Decide whether `test:board:live` should be documentation-only for now or backed by a small guarded test file such as `tests/task-source-live-smoke.test.mjs`. <!-- sha:22c5d11 -->
- [x] Task 2.2: If a live smoke test is added, require explicit environment prerequisites before any Forge run is created, using existing `.env.forge`/GitLab conventions from `runtime/task-source-config.mjs` and `runtime/task-source-gitlab.mjs`. <!-- sha:22c5d11 -->
- [x] Task 2.3: Ensure the live smoke path only verifies connectivity, task readability, required marker label behavior, and fail-fast diagnostics; it must not be part of `npm.cmd test`. <!-- sha:22c5d11 -->

### Verification

- [x] Running the live smoke command without prerequisites exits with clear skip or missing-config guidance.
- [x] The default mocked board suite remains independent of network access.
- [x] No live-board code path persists `TASK_SOURCE_TOKEN` into run artifacts or test output.

## Phase 3: Docs & Cleanup

Make the testing model visible so future agents and humans do not debug the wrong layer.

### Tasks

- [x] Task 3.1: Update `README.md` with a "Testing" section covering command names, Windows `npm.cmd` guidance, and failure interpretation by test layer. <!-- sha:2ca8a28 -->
- [x] Task 3.2: Document that real board validation is optional smoke coverage and should be run only with a prepared task, marker label, token, and known non-production tracker state. <!-- sha:a5241f2 -->
- [x] Task 3.3: Remove obsolete or misleading scripts after confirming no README references point to old names. <!-- sha:4248afe -->

### Verification

- [ ] README accurately reflects scripts in `package.json`.
- [ ] Linter is not applicable unless a lint script is added.
- [ ] `npm.cmd test` passes.

## Final Verification

- [ ] All acceptance criteria from spec met.
- [ ] `npm.cmd test` passes.
- [ ] New named scripts pass or clearly skip guarded live smoke behavior.
- [ ] Documentation is up to date.

## Context Handoff

### Session Intent

Create a stable Forge testing strategy that separates deterministic workflow e2e from optional live task-board smoke validation.

### Key Files

- `package.json`
- `README.md`
- `tests/forge-runner.test.mjs`
- `tests/mcp-server.test.mjs`
- `tests/task-source-mcp.test.mjs`
- `tests/task-source-gitlab.test.mjs`
- `tests/task-label-sync.test.mjs`
- `tests/forge-board-labels.test.mjs`
- Optional: `tests/task-source-live-smoke.test.mjs`

### Decisions Made

- Default regression stays deterministic and uses mocked task-board behavior.
- Live tracker validation is opt-in smoke coverage, not required for every e2e run.
- Windows users should run scripts through `npm.cmd` when PowerShell blocks `npm.ps1`.

### Risks

- Script names can drift from README if not verified together.
- A live-board smoke test can become flaky if it validates too much external state.
- Existing draft plans around GitLab fail-fast and task gating overlap in behavior; this track should depend on them, not reopen their implementation scope.

---
_Generated by /plan. Tasks marked [~] in progress and [x] complete by /build._
